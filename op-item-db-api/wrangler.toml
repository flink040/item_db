name = "op-item-db-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[vars]
SUPABASE_URL = "https://pgrtmdxmewsxixzubqpr.supabase.co"

# ENTWEDER: Standalone Worker, der /api/* auf deiner Domain bedient:
routes = [
  { pattern = "op-item-db.com/api/*", zone_name = "op-item-db.com" }
]

# Falls supabase-js doch Node-APIs braucht:
# node_compat = true

# Normale (nicht geheime) Variablen:
[vars]
SUPABASE_URL = "https://<dein-projekt>.supabase.co"
